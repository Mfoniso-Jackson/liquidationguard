# Shared Package

This package is reserved for shared schemas and types as LiquidationGuard grows.

For v1, the backend Pydantic schemas in `apps/api/app/schemas.py` and the frontend TypeScript types in `apps/web/lib/types.ts` are intentionally mirrored but kept simple. If the contract grows, generate or centralize API types here.
